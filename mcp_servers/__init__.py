"""Standalone MCP servers exposing business capabilities as tools.

Each subpackage is an independently runnable server built on the official MCP
Python SDK. They can be launched as stdio subprocesses (the default) or deployed
as separate Streamable HTTP services.
"""
